RewriteEngine On

# /privacy → privacy.html, /terms → terms.html (адрес в браузере остаётся без .html)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{DOCUMENT_ROOT}/$1.html -f
RewriteRule ^([^/.]+)/?$ /$1.html [L]
